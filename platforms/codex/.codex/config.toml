# Codex CLI configuration generated by the Agentic Development System adapter.
# Re-run scripts/sync-platforms.sh codex to regenerate.

model = "gpt-5-codex"

# Human-gate everything that touches the filesystem or runs commands.
# This is the Codex equivalent of the orchestrator's confidence gate.
approval_policy = "on-request"

[sandbox]
mode = "workspace-write"

[project]
agents_file = "AGENTS.md"
