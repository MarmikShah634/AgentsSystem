# Feature Spec — `<feature-name>`

## Goal
<one sentence>

## User Stories
- **US-1** — As a `<role>`, I want `<capability>` so that `<value>`.

## Acceptance Criteria
- **AC-1** (US-1) — Given `<context>`, when `<action>`, then `<outcome>`.

## Out of Scope
- ...

## Open Questions
- ...

## Risks
- ...
